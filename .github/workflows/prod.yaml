name: PROD CI/CD
on: 
  release:
    types: [published]

jobs: 
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
        
      - name: test console
        run: |
          echo "test console"